[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-ran-kpi-forecasting"
version = "0.1.0"
description = "AI-RAN KPI forecasting and telecom observability report generation."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.23.0",
  "pandas>=1.5.0",
]

[project.scripts]
ai-ran-kpi-forecasting = "ai_ran_kpi_forecasting.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
