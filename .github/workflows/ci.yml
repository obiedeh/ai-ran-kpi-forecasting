name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run lint
        run: |
          ruff check .

      - name: Run tests
        run: |
          pytest -q

      - name: Generate benchmark report bundle
        run: |
          python ai-ran-kpi-forecasting.py forecast \
            --data data/ran_kpi_sample.csv \
            --cell-id CELL_001 \
            --kpi-col prb_dl_util \
            --horizon 24 \
            --output-dir reports/forecast_examples/latest

      - name: Verify report artifacts
        run: |
          test -f reports/forecast_examples/latest/prb_dl_util_forecast.svg
          test -f reports/forecast_examples/latest/prb_dl_util_impact.svg
          test -f reports/forecast_examples/latest/metrics_summary.md

      - name: Generate congestion scenario dashboard
        run: |
          python ai-ran-kpi-forecasting.py scenario-demo \
            --output-dir reports/scenarios/latest

      - name: Verify scenario artifacts
        run: |
          test -f reports/scenarios/latest/dashboard/dashboard.html
          test -f reports/scenarios/latest/dashboard/dashboard_summary.md
          test -f reports/scenarios/latest/baseline/prb_dl_util_impact.svg
          test -f reports/scenarios/latest/congestion/prb_dl_util_impact.svg
